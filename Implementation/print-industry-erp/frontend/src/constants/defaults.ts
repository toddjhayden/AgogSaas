// Default values for the application
// These are fallback values when data is not available

export const DEFAULTS = {
  // Fallback tenant ID when no tenant is selected
  TENANT_ID: 'system',

  // Fallback facility ID when no facility is selected
  FACILITY_ID: 'main',

  // Fallback user ID for system operations
  USER_ID: 'system',
} as const;
