# AgogSaaS Monitoring Configuration
# Copy this file to .env and configure

# =============================================================================
# GRAFANA CONFIGURATION
# =============================================================================

# Admin credentials (CHANGE THESE!)
GRAFANA_USER=admin
GRAFANA_PASSWORD=changeme  # Use strong password in production!

# =============================================================================
# DATABASE CONFIGURATION (for exporters)
# =============================================================================

DB_USER=agogsaas_user
DB_PASSWORD=changeme  # Must match main database password
DB_NAME=agogsaas

# =============================================================================
# REGION CONFIGURATION
# =============================================================================

REGION=US-EAST  # Options: US-EAST, EU-CENTRAL, APAC

# =============================================================================
# ALERTING CONFIGURATION
# =============================================================================

# Slack (for alerts)
# Create webhook: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# PagerDuty (for critical alerts)
# Get integration key from PagerDuty service settings
PAGERDUTY_SERVICE_KEY=your-pagerduty-integration-key

# Email (SMTP for alerts)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=alerts@agogsaas.com
SMTP_PASSWORD=your-email-password

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Microsoft Teams webhook
TEAMS_WEBHOOK_URL=

# Discord webhook
DISCORD_WEBHOOK_URL=

# Twilio (for SMS/phone alerts to facility IT staff)
TWILIO_SID=
TWILIO_TOKEN=
TWILIO_FROM_NUMBER=+1234567890

# =============================================================================
# SECURITY NOTES
# =============================================================================
#
# 1. NEVER commit .env file to git (it's in .gitignore)
# 2. Use strong passwords (generate with: openssl rand -base64 32)
# 3. Rotate credentials regularly
# 4. Use environment-specific secrets in production
# 5. Consider using a secrets manager (HashiCorp Vault, AWS Secrets Manager)
#
