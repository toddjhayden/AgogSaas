# SDLC Platform Environment
# Copy to .env and fill in your values
# For use with sdlc/docker-compose.yml
# Database operations go through VPS API (no local postgres)

# Node environment
NODE_ENV=development

# NATS Authentication (local NATS for messaging)
NATS_USER=agents
NATS_PASSWORD=your-secure-nats-password-here

# SDLC Control - VPS API (PRIMARY)
# All database operations go through this API
SDLC_API_URL=https://api.agog.fyi
SDLC_AGENT_ID=local-sdlc-core

# Ollama (local for embeddings)
OLLAMA_URL=http://ollama:11434

# SDLC Agent Personas (relative to /app in container)
SDLC_AGENTS_DIR=/app/agents/personas

# Project workspace root (relative to /app in container)
PROJECT_ROOT=/workspace/projects
